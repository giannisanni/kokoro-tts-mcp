[project]
name = "kokoro-tts-mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "kokoro>=0.8.4",
    "mcp[cli]>=1.3.0",
    "soundfile>=0.13.1",
]
